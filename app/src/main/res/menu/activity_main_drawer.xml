<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group>
        <item android:id="@+id/nav_home"
            android:icon="@drawable/ic_home"
            android:title="Home"/>

        <item android:id="@+id/nav_add"
            android:icon="@drawable/ic_add_member"
            android:title="Add Member"/>

        <item android:id="@+id/nav_fee_pending"
            android:icon="@drawable/ic_fees_outstanding"
            android:title="Fee Pending"/>
    </group>

    <item android:title="Setting">
        <menu>
            <item android:id="@+id/nav_update_fee"
                android:icon="@drawable/ic_cost_seting"
                android:title="Add / Update Fee"/>

            <item android:id="@+id/nav_change_password"
                android:icon="@drawable/ic_setting"
                android:title="Change Password / Mobile"/>

            <item android:id="@+id/nav_log_out"
                android:icon="@drawable/ic_log_out"
                android:title="Log Out"/>
        </menu>
    </item>

</menu>